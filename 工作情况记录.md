# Vortix Kitchen 独立站 — 工作情况记录

> 最后更新：**2026-08-26 10:30** ｜ 维护人：雪姐（Shirley）｜ 助手：WorkBuddy

---

## 一、项目概况

- **品牌**：Vortix Kitchen（原 Vertex，已重命名）
- **公司**：Foshan Vortix Co., LTD
- **站点**：https://vortixkitchen.com （GitHub Pages 托管）
- **仓库**：`shirleyleung5616-crypto/vortixkitchen`（公开仓库，`main` 分支 push 即上线）
- **本地目录**：`E:\新桌面\做网站\网站目录`
- **目标市场**：中亚 / 俄罗斯 / 东南亚 / 非洲，B 端采购商、品牌商、批发商（**不做欧美、不做中东**）
- **联系**：WhatsApp +86 13790093901 ｜ 邮箱 Shirley20193@163.com
- **定位**：纯 B2B 询盘站，**不显示价格、不放购物车**，WhatsApp 优先询盘

---

## 二、当前站点状态（2026-08-26）

| 项目 | 数值 |
|---|---|
| 博客文章 | **20 篇**（16 篇单痛点 `pain-*` + 4 篇 `article-*` 决策指南 + W2 清关/电压篇 + W3 安全付款篇） |
| sitemap URL | **22 个**（首页 + blog/index + 20 篇） |
| 文章字数 | 全部 ≤200 英文词、纯利他、问题导向、结尾带 WhatsApp/邮件 CTA |
| 结构化数据 | 每篇带 canonical + Open Graph 社交标签；电压篇带 FAQPage JSON-LD |
| 首页 SEO | 已补 canonical + og 标签（部署 d90020e） |

**发布铁律（雪姐硬标准，勿偏离）**：
1. 一篇只写一个痛点，≤200 词，零废话
2. **每天整站最多只发 1 篇**，绝不一天批量发（题材有限，硬凑=灌水）
3. 雪姐给的批量卖点 → 全部进 `blog/backlog.md` 待写池，由每日自动化每天取第一篇，天然错开
4. 不写价格、不写 ISO、不写认证地图/说明书式填充物

---

## 三、搜索收录状态

| 引擎 | 现状 | 推送方式 |
|---|---|---|
| **Bing / Yandex** | 靠 IndexNow 催索引（密钥 50f220d962d0f9f8b86fdeb0af825360） | 每次部署自动 POST，实测 HTTP 200 |
| **Google** | 自 2026-08-12 域名上线起持续 0 收录（非技术阻挡，缺外链信号） | **GSC API 已正式接通**（2026-08-26），首次提交 HTTP 204 成功 |

**GSC 已接通（2026-08-26 一次性设置完成）**：
- 服务账号：`vortix-gsc-bot@active-alchemy-461308-d5.iam.gserviceaccount.com`（GSC 拥有者）
- 密钥文件：项目根 `gsc_service_account.json`（已 gitignore，安全）
- 提交脚本：`gsc_submit.py`（用 requests+pyjwt，此环境 googleapiclient 会超时）
- 谷歌公开 ping 接口已失效（404/410），GSC API 是唯一可靠杠杆

---

## 四、自动化配置（雪姐零操作）

### 每日自动化 `automation-1787706480768`（ACTIVE, FREQ=DAILY）
每天执行闭环：
1. 读 `blog/backlog.md` 第一个未勾选项 → 按铁律写 1 篇（若今天已发过则停写）
2. 部署（git push，带 `-c credential.helperselector= -c credential.helper=` 防无头卡死）
3. IndexNow（Bing/Yandex）催索引
4. **GSC API 正式提交 sitemap（Google）**
5. WebSearch 查 `site:vortixkitchen.com` 覆盖率
6. 未收录逐条追原因（robots/noindex/canonical/200/外链）
7. 中文汇报：总数/已收/覆盖率/未收清单+原因/今日动作

### 周自动化 `automation-1787278422603`（ACTIVE, FREQ=WEEKLY 周一）
每周一部署本地改动 + 提交 IndexNow 催 Bing/Yandex。

---

## 五、关键文件速查

| 文件 | 用途 |
|---|---|
| `index.html` | 主页（含 Organization+FAQ JSON-LD） |
| `blog/build_blog.py` | 博客生成器，ARTICLES 列表驱动，加文章就改这里 |
| `blog/backlog.md` | 痛点素材池 + 上线记录（新卖点先丢这里） |
| `blog/*.html` | 生成的博客文章 |
| `sitemap.xml` / `robots.txt` / `llms.txt` | SEO/GEO 核心文件 |
| `gsc_service_account.json` | GSC API 密钥（**勿提交 git**） |
| `gsc_submit.py` | GSC sitemap 提交脚本（venv 运行） |
| `.github_token` | GitHub PAT（**勿提交 git**） |
| `README.md` | 含官网外链，借 GitHub 高频爬取带谷歌顺藤摸瓜 |

---

## 六、新窗口接手指引（如换会话/换窗口）

1. 读本文件 + `.workbuddy/memory/MEMORY.md`（项目长期约定）
2. 如需写新博客：把卖点丢进 `blog/backlog.md` 待写池（保持 `[ ]` 未勾选），**不要手动同天批量发**，交给每日自动化
3. 如需手动发：在 `blog/build_blog.py` 的 ARTICLES 列表末尾追加 dict，重跑 `python blog/build_blog.py`，再 `git push`
4. GSC 提交已自动化，无需手动；如需单独跑：`venv python gsc_submit.py`
5. 查收录：WebSearch `site:vortixkitchen.com`

---

## 七、待办 / 下一步

- [x] GSC 一次性设置完成，sitemap 首次提交成功（HTTP 204）
- [x] 每日自动化升级为全自动收录闭环
- [x] 域名 2026-08-12 已上线（GitHub Pages + CNAME 绑定）；至今 16 天谷歌仍 0 收录
- [ ] 持续铺外链/曝光信号（LinkedIn/阿里/邮件签名/社媒），催谷歌首次爬行与索引
- [ ] backlog 下一待写项：商用 vs 家用、集装箱装载量等（每天由自动化取一篇）
- [ ] 主动获客话术（WhatsApp/邮件引流）雪姐尚未拍板，待确认
